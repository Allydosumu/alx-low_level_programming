#include "main.h"

/**
 * _abs - This program writes a function that
 * computes the absolute value of an integer
 *
 * @d: d is an integer
 *
 * Return: 0
 *
 */
int _abs(int d)
{
	if (d > 0)
	{
		return (d);
	}
	else if (d < 0)
	{
		return (-d);
	}
	else
	{
		return (d);
	}
}
